import React from "react";
const Pattern = ({ helloVar }) => {
  let letname = 100;
  var varname = 100;

  console.log(letname, varname);

  return <h2>Pattern {helloVar}we swds</h2>;
};

export default Pattern;
